<Window x:Class="YoutubeExplodeDemo.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="YoutubeExplode Demo"
        Width="600"
        Height="500"
        Background="{DynamicResource MaterialDesignPaper}"
        DataContext="{Binding Main,
                              Source={StaticResource Locator}}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.FontSize="13"
        TextElement.FontWeight="Regular"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d ignore">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Youtube Video ID or URL:" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="tbVideoID"
                             Grid.Column="0"
                             Text="{Binding VideoID}" KeyDown="tbVideoID_KeyDown" />
                    <Button x:Name="btnSubmit"
                            Grid.Column="1"
                            Command="{Binding SubmitCommand}"
                            Content="Go" />
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1" Visibility="{Binding VideoInfoVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}">
                    <TextBlock Margin="5"
                               HorizontalAlignment="Center"
                               FontSize="20"
                               Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                               Text="{Binding VideoInfo.Title}" />
                </Border>

                <Image Grid.Row="1"
                       Margin="5"
                       Source="{Binding ThumbnailImageSource}" />

                <UniformGrid Grid.Row="2"
                             Margin="5"
                             Columns="4">
                    <TextBlock HorizontalAlignment="Center" Text="{Binding VideoInfo.Author}" />
                    <TextBlock HorizontalAlignment="Center" Text="{Binding VideoInfo.Length}" />
                    <TextBlock HorizontalAlignment="Center" Text="{Binding VideoInfo.ViewCount, StringFormat=Views: \{0\}}" />
                    <TextBlock HorizontalAlignment="Center" Text="{Binding VideoInfo.AvgRating, StringFormat=Rating: \{0:N2\}}" />
                </UniformGrid>

                <UniformGrid Grid.Row="3" Columns="2">
                    <ComboBox Margin="5"
                              ItemsSource="{Binding VideoInfo.Streams}"
                              SelectedItem="{Binding SelectedStream}" />
                    <Button Margin="5"
                            Command="{Binding DownloadVideoCommand}"
                            Content="Download" />
                </UniformGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
